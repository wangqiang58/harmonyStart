import { CommonConstants } from '../../constants/CommonConstants'

@Entry
@Component
export struct HotPage {
  build() {
    Column() {

    }.backgroundColor(Color.Blue).width(CommonConstants.FULL_WIDTH_PERCENT).height(CommonConstants.FULL_HEIGHT_PERCENT)

  }
}