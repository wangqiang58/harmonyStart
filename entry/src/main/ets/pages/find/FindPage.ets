import { CommonConstants } from '../../constants/CommonConstants'

@Entry
@Component
export struct FindPage {
  build() {
    Column() {

    }.width(CommonConstants.FULL_WIDTH_PERCENT).height(CommonConstants.FULL_HEIGHT_PERCENT).backgroundColor(Color.Pink)

  }
}